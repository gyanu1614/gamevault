# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Personal folders - never commit
MDFiles_Archive/
progress/
# Root-level test/debug scripts
add_test_code.js
check_*.mjs
run-migration.js
run_migration.js
test_*.mjs
verify_*.js
*.backup
supabase/.temp/
supabase/migrations/RUN_THIS_*.sql
supabase/migrations/check_listing_status.sql
supabase/migrations/fix-admin-roles-rls.sql
CODING-STANDARDS.md

MDFiles_Archive/
OldProgress/
.claude/
.next-check/
.next-check2/
design-refs/

data/sab-market-imports/*.csv
!data/sab-market-imports/template.csv

# Collector debug dumps (per-run crawl artifacts)
data/sab-market-feeds/eldorado-debug/

# Local-only planning doc (PSP onboarding) — never tracked
HANDOFF_PSP_ONBOARDING.md

# ── Working artifacts: docs, legal, planning, design & data dumps ──
# Kept out of the app repo so they never clutter `git status` or a deploy.
# (Move any such files into these folders / patterns going forward.)
HANDOFF_*.md
PLAN_*.md
psp-onboarding/
design/
email-templates/
skills-lock.json
# Archived (non-CLI-managed) SQL migrations — history only, never deployed
supabase/migrations_archive/
# Bulk crawl/price data feeds — regenerated by collectors, not app source
data/adopt-me-feeds/*.json
data/sab-market-feeds/*.json
# Claude Code project scaffolding that isn't app code
.github/agents/
.github/hooks/
.github/skills/
.agents/
